{<% var ngVer = "1.3.0" %>
  "name": "<%= _.slugify(_.humanize(appname)) %>",
  "version": "0.0.0",
  "dependencies": {
    "angular": "^<%= ngVer %>",
    "angular-route": "^<%= ngVer %>"<% if (bootstrap) { %>,<% if (!compassBootstrap) { %>
    "bootstrap": "^3.2.0"<% } else { %>
    "bootstrap-sass-official": "^3.2.0"<% } } %>,
    "tecnotree-ui-library": "http://10.4.60.111:8085/tec_common/ui-lib.git#master"
  },
  "resolutions": {
    "angular": "~1.3.16"
  },
  "devDependencies": {
    "angular-mocks": "^<%= ngVer %>"
  }<% if (appPath) { %>,
  "appPath": "<%= appPath %>"<% } %>,
  "moduleName": "<%= scriptAppName %>"
}
